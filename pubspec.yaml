name: moodle_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  form_field_validator: ^1.1.0
  fluttertoast: ^8.0.9
  image_picker: ^0.8.7 # Use the latest version
  file_picker: ^5.0.0
  stripe_payment: ^1.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  assets:
    - assets/images/moodle.png
  uses-material-design: true
