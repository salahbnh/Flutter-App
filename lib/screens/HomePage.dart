import 'package:flutter/material.dart';
import '../widgets/Bottom_navigation.dart'; // Ensure this path is correct
import '../widgets/custom_appBar.dart'; // Ensure this path is correct

class HomePScreen extends StatefulWidget {
  const HomePScreen({super.key});

  @override
  State<HomePScreen> createState() => _HomePScreenState();
}

class _HomePScreenState extends State<HomePScreen> {
  // Sample list of courses


  @override
  Widget build(BuildContext context) {
    return Container(
      // Customize your bottom navigation

    );
  }
}

// Example CourseDetailScreen (you'll need to create this screen)

